version: 1
frontend:
  phases:
    preBuild:
      commands:
        - echo "Static site build — no dependencies to install"
    build:
      commands:
        - npm run build --prefix apps/frontend
  artifacts:
    baseDirectory: apps/frontend/dist
    files:
      - '**/*'
  cache:
    paths: []
