# Frontend Archive Relocation

このリポジトリの最新のフロントエンド実装は `old/apps/frontend/` に完全な形で保管されています。
ビルドや依存関係を確認したい場合は、`old/` 以下の従来プロジェクトを参照してください。

この `apps/frontend` ディレクトリは、過去のパスを参照するスクリプトやツールでエラーが出ないようにプレースホルダーとして残しています。
